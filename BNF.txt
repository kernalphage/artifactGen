definition -> "[" LITERAL "]" statement*
// not sure if i want that ":"
statement -> target_expr ":" value_expr
target_expr -> target_id* 

// todo: expand with an expression that allows @(#location_to_modify)
// or "@material.place
target_id -> ("@","$") locator_expr

// person.height.cm for example 
locator_expr -> list(LITERAL, ".")

value_expr -> list( list(value, ";"), "|") 
// +100 or 
value -> base_expr | side_effect 
base_expr -> LITERAL | STRING | NUMBER | number_expr | definition_ref | tag_ref


number_expr -> NUMBER | list(NUMBER, ":")
side_effect -> TODO
definition_ref -> "#" locator_expr
tag_ref -> "{" locator_expr "}"
function -> "!" locator_expr "(" list(base_expr, ",") ")"


/// definition: a group of statements will be applied to the  
// statement: a list of  